Thin drivers only — imports, calls, plots. Logic lives in `engine/`.

Run `nbstripout --install` in the repo root before committing so notebook outputs
do not pollute diffs. Charts worth keeping go in `benchmarks/plots/` as PNGs.
